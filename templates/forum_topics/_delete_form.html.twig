<form method="post" action="{{ path('forum_topics_delete', {'id': forum_topic.id}) }}">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ forum_topic.id) }}">
    <button class="btn btn btn-outline-danger btn-sm">supprimer</button>
</form>
